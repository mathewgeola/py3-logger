[project]
name = "py3-logger"
version = "0.0.8"
description = "python3 logger"
readme = "README.md"
authors = [
    { name = "mathewgeola", email = "mathewgeola@gmail.com" }
]
requires-python = ">=3.12.10"
dependencies = [
    "colorlog>=6.10.1",
]

[build-system]
requires = ["uv_build>=0.11.7,<0.12.0"]
build-backend = "uv_build"
